# 开发环境留空，走 Vite 同源代理，避免 CORS 的 Failed to fetch
# 生产环境可改为真实 API 根地址，例如 https://api.example.com
VITE_API_BASE_URL=

# 每日推荐接口（需登录），开发时由 Vite 代理到 localhost:3000
VITE_DAILY_RECOMMENDATIONS_API=/recommend/songs

# 仅 vite.config.ts 使用：开发代理目标地址
VITE_API_PROXY_TARGET=http://localhost:3000
